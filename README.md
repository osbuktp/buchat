# buchat

Chat application (It's not ready. You can try and run it if you want,
but I recommend you to watch memes instead)

# Ready
- base slices (slices combine reducers and action creators in one entity)
- layout and routing (layouts to be changed when I find decent desing)

# Todo
- adequate design instead of randomly placed bootstrap components
- components' functionality
- apis (data fetching)
- actions for slices
- p2p video communication using webrtc